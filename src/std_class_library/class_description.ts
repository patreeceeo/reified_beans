import { type CompiledClass } from "src/virtual_objects";

const dClassDescription: CompiledClass = {
  name: "ClassDescription",
  superClass: "Behavior",
  ivars: [],
  classComment:
    "My instances provide methods that access classes by category, and allow whole categories of classes to be filed out to persistent storage.",
  methodDict: {},
};

export default dClassDescription;
